# Dotnet in AWS
### Description
C# and Dotnet Core workload running on AWS Compute Environment and using AWS Services.

### Tools
####  cfn-lint
Install the `cfn-lint` utility for validating CloudFormation template
```bash
$ pip install cfn-lint
```

#### CloudFormation Linter VSCode Extension
Install the _CloudFormation Linter_ VSCode extension.  
